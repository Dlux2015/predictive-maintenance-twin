# =============================================================================
# Predictive Maintenance Digital Twin – Python dependencies
# Target environment: Python 3.9+  |  AWS + Databricks (Wavicle stack)
#
# Pin policy:
#   - Runtime deps: pinned to known-good minor version (==X.Y.*)
#   - Dev/type-stub deps: pinned tightly so CI is reproducible
#   - PySpark / Delta: kept in lockstep with Databricks Runtime 14.x LTS
#     (DBR 14.x ships Spark 3.5 + Delta 3.x)
# =============================================================================

# ── AWS SDK ──────────────────────────────────────────────────────────────────
boto3==1.34.*                  # S3, Kinesis, CloudWatch, IAM, Firehose

# ── Configuration & HTTP ─────────────────────────────────────────────────────
pyyaml==6.0.*                  # YAML config loading (ingestion/config.yaml)
requests==2.32.*               # ThingSpeak REST polling + general HTTP

# ── Spark / Delta Lake ───────────────────────────────────────────────────────
pyspark==3.5.*                 # Matches Databricks Runtime 14.x (LTS)
delta-spark==3.2.*             # Delta Lake OSS; pin to DBR-compatible release

# ── Databricks ───────────────────────────────────────────────────────────────
databricks-sdk==0.28.*         # Workspace, Jobs, Clusters, Workflows APIs
databricks-sql-connector==3.3.*  # SQL connector for querying Databricks SQL

# ── Data Quality ─────────────────────────────────────────────────────────────
great_expectations==0.18.*     # Expectation suites, checkpoints, data docs

# ── Resilience ───────────────────────────────────────────────────────────────
tenacity==8.3.*                # Retry / exponential-backoff decorator

# ── Environment / secrets ────────────────────────────────────────────────────
python-dotenv==1.0.*           # Load .env for local dev (never commit .env)

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.*                  # Test runner
pytest-cov==5.0.*              # Coverage reports

# ── Code quality & type checking ─────────────────────────────────────────────
black==24.4.*                  # Opinionated formatter
mypy==1.10.*                   # Static type checker

# ── Type stubs (mypy companions) ─────────────────────────────────────────────
types-requests==2.32.*         # Stubs for `requests`
types-PyYAML==6.0.*            # Stubs for `pyyaml`
